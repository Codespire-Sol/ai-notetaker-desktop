# Codespire Notetaker — optional env overrides (dev convenience).
# In production these are entered in the app's Settings screen instead.

OPENAI_API_KEY=
SARVAM_API_KEY=

# Microsoft Teams (Azure app registration — Application/client ID)
MS_CLIENT_ID=

SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=
